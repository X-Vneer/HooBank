import React from "react";

const Button = () => (
  <div>
    <button className="capitalize px-4 py-2 block w-fit rounded bg-blue-gradient font-semibold mx-auto md:mx-0">
      get started
    </button>
  </div>
);

export default Button;
